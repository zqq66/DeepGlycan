name: deepglycaninference
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      # Keep this file small and focused: direct imports only (excluding `helper/`).
      # For a fully reproducible lockfile, prefer `conda env export` or `pip freeze`.
      - -r requirements.txt
